@model IEnumerable<Quotation>


<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Quotation Overview</h5>

                    <!-- Table with stripped rows -->
                    
                    <table class="table datatable ">
                        <thead>
                            <tr class="">
                                <th class="text-start">Order Request No</th>
                                <th class="text-start">Quotation No</th>
                                <th class="text-start">Status</th>
                                <th class="text-start">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var quotation in Model)
                            {
                                <tr class="">
                                    <td class="text-start">@quotation.RequestNo</td>
                                    <td class="text-start"> @quotation.QuotationNo</td>
                                    <td class="text-start">
                                        @switch (quotation.Status)
                                        {
                                            case "Pending":
                                                <span class="badge bg-warning">@quotation.Status</span>
                                                break;
                                            case "Cancelled":
                                                <span class="badge bg-danger">@quotation.Status</span>
                                                break;
                                            case "Approved":
                                                <span class="badge bg-success">@quotation.Status</span>
                                                break;
                                            case "Processing":
                                                <span class="badge bg-info">@quotation.Status</span>
                                                break;
                                            default:
                                                @quotation.Status
                                                break;
                                        }
                                    </td>
                                    @if (quotation.Status == @SD.StatusPending)
                                    {
                                        <td class="text-start">

                                            <form method="post" asp-action="QuotationIndexPost" asp-controller="Quotation" class="d-inline">
                                                <input type="hidden" name="id" value="@quotation.Id" />
                                                <input type="hidden" name="status" value="@SD.StatusInProcess" />
                                                <button type="submit" class="btn btn-success btn-sm">Approve</button>
                                            </form>
                                            <form method="post" asp-action="QuotationIndexPost" asp-controller="Quotation" class="d-inline">
                                                <input type="hidden" name="id" value="@quotation.Id" />
                                                <input type="hidden" name="status" value="@SD.StatusCancelled" />
                                                <button type="submit" class="btn btn-danger btn-sm">Reject</button>
                                            </form>
                                        </td>
                                    }
                                    else
                                    {
                                        <td></td>

                                    }
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</section>
